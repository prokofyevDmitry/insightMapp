<?php


// les types des entrées:
// on suppose que tout les données reçus sont remplis, un script en javascript va s'occuper de tester la bonne présence des variables.
/*nomchamp:alias
 * nom
 * prenom
 * mail : filter_var($mail, 'FILTER_VALIDATE_EMAIL');
 * mdp
 * repetition mdp
 * 
 */

function test_input($test, $methodeS)
{
	$type_methode = array( 
	'nom','prenom','mail'	
	);
	$result = false;
	
	
	
	
		switch($methode) // l'ordre est défini par le POST
		{
			case 'nom': $result = test_name($input); break;
			case 'prenom':$result = test_name($input); break;
			case 'mail':$result = filter_var($string,'FILTER_VALIDATE_EMAIL'); break;
			
		}
	// test nom et prénom: pas de digitales, pas de caractères spéciaux. 
	function test_name($string)
	{
	$string = preg_replace('.-.', '', $string);
		$string = preg_replace('. .', '', $string);
		echo $string .'<br>	';
		if(preg_match('#^[A-z]+$#',$string))
		return 0; // si on trouve quelque chose alors on va envoyer 0 (on cherche un chifre)
		else return 1;
	}

	
return $result; 	
	
	
}


